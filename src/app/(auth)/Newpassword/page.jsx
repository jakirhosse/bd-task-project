import Newpassword from "@/components/auth/Newpassword";
import React from "react";

const page = () => {
  return (
    <div>
      <Newpassword></Newpassword>
    </div>
  );
};

export default page;
